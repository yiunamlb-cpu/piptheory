name: Daily Strength Update

# Retired the LLM "bias engine" (research desk) to cut spend. This now only
# recomputes the mechanical, free currency-strength meter and commits it.

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  strength:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Compute currency strength meter
        env:
          PYTHONPATH: ${{ github.workspace }}
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
        run: python -m src.data.currency_strength --persist

      - name: Compute price-based currency performance
        continue-on-error: true
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: python -m src.data.price_strength --persist

      - name: Commit and push results
        run: |
          git config user.name "piptheory-bot"
          git config user.email "bot@piptheory.com"
          git add --force state/currency_strength.json state/currency_strength_latest.json state/price_strength.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "state: daily strength update"
          git pull --rebase origin main
          git push origin main

      - name: Warm up Render instance after deploy
        run: |
          sleep 60
          for i in 1 2 3 4 5; do
            curl -s -o /dev/null -w "HTTP %{http_code}\n" --max-time 120 https://piptheory.com/ || echo "Request failed"
            sleep 60
          done
