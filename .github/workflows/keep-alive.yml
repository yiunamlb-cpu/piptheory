name: Keep Alive

on:
  schedule:
    - cron: "*/4 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping piptheory.com
        run: |
          curl -s -o /dev/null -w "Pinged piptheory.com — HTTP %{http_code}\n" --max-time 60 https://piptheory.com/